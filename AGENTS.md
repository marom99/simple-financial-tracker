# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

## Design Context

Read `PRODUCT.md` before any UI or design work. Register: **product** (app UI — design serves the tool). Key principles: native iOS feel, seconds-to-log expense capture, clarity over density, honest scope, local-by-default. Visual tokens live in `src/theme/index.ts`; run `$impeccable document` to generate `DESIGN.md` when ready.
