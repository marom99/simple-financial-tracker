"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm install"
run_mode = "concurrent"

[scripts.run.dev]
command = "expo start --port $CONDUCTOR_PORT"
default = true
icon = "play"

[scripts.run.test]
command = "npx jest --watch"
icon = "test-tube"
